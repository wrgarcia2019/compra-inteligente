
export const APP_TITLE = "Smart Grocery List";
export const DEFAULT_BUDGET = 100;

// Tailwind CSS classes for consistent styling
export const PRIMARY_COLOR = "sky"; // e.g., bg-sky-500, text-sky-400
export const SECONDARY_COLOR = "slate";

export const INPUT_CLASS = `bg-${SECONDARY_COLOR}-700 border border-${SECONDARY_COLOR}-600 text-${SECONDARY_COLOR}-100 text-sm rounded-lg focus:ring-${PRIMARY_COLOR}-500 focus:border-${PRIMARY_COLOR}-500 block w-full p-2.5 placeholder-${SECONDARY_COLOR}-400`;
export const BUTTON_PRIMARY_CLASS = `bg-${PRIMARY_COLOR}-600 hover:bg-${PRIMARY_COLOR}-700 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:shadow-outline transition-colors duration-150`;
export const BUTTON_SECONDARY_CLASS = `bg-${SECONDARY_COLOR}-600 hover:bg-${SECONDARY_COLOR}-700 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:shadow-outline transition-colors duration-150`;
export const CARD_CLASS = `bg-${SECONDARY_COLOR}-800 shadow-xl rounded-lg p-6`;
    